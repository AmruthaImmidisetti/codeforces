#include <bits/stdc++.h>using namespace std; int main() {    int t;    cin >> t;    while (t--)    {        int n;        cin>>n;        string s2;        cin >> s2;        string s1 = "Timur";        int f = 0;        if (n != s1.size()) cout << "NO" << endl;        else {            sort(s1.begin(), s1.end());            sort(s2.begin(), s2.end());            for (int i = 0; i < n; i++) {                if (s1[i] != s2[i]) {                    f = 1;                    break;                }            }            (f) ? cout << "NO" << endl: cout << "YES" << endl;        }    }}